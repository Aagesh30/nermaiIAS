import { db } from '../infrastructure/firebase/index'; async function run() { const snap = await db.collection('live_sessions').get(); for (const doc of snap.docs) { await doc.ref.set({ joinedParticipantsDetails: { 'test-student-1': { id: 'test-student-1', name: 'Test Student', role: 'student', regNo: '12345', joinedAt: new Date().toISOString() } } }, { merge: true }); console.log('Updated', doc.id); } process.exit(0); } run().catch(e => { console.error(e); process.exit(1); });
