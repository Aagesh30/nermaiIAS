const admin = require('firebase-admin'); if (!admin.apps.length) { admin.initializeApp({ credential: admin.credential.applicationDefault() }); } const db = admin.firestore(); async function run() { const doc = await db.collection('classes').doc('s2A7CyRuWMKrJZFOJbvE').get(); if (doc.exists) { console.log('classes doc:', doc.data()); } else { console.log('not in classes'); } } run().then(() => process.exit(0)).catch(e => { console.error(e); process.exit(1); });
