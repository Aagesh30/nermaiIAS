const admin = require('firebase-admin'); if (!admin.apps.length) { admin.initializeApp({ credential: admin.credential.applicationDefault() }); } const db = admin.firestore(); async function run() { const doc = await db.collection('live_sessions').doc('wXaCcwcFMGmMSL2BP1fA').get(); if (doc.exists) { console.log('attendance details:', doc.data().joinedParticipantsDetails); } else { console.log('doc not found'); } } run().then(() => process.exit(0)).catch(e => { console.error(e); process.exit(1); });
