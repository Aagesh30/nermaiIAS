const admin = require('firebase-admin'); if (!admin.apps.length) { admin.initializeApp({ credential: admin.credential.applicationDefault() }); } const db = admin.firestore(); async function run() { const doc = await db.collection('live_sessions').doc('s2A7CyRuWMKrJZFOJbvE').get(); if (doc.exists) { console.log('live_sessions doc:', doc.data()); } else { console.log('not in live_sessions'); } } run().then(() => process.exit(0)).catch(e => { console.error(e); process.exit(1); });
