const admin = require('firebase-admin'); if (!admin.apps.length) { admin.initializeApp({ credential: admin.credential.applicationDefault() }); } const db = admin.firestore(); async function run() { const snap = await db.collection('classes').where('title', '==', 'IVC-2').get(); snap.forEach(doc => console.log('IVC-2 id:', doc.id)); } run().then(() => process.exit(0)).catch(e => { console.error(e); process.exit(1); });
