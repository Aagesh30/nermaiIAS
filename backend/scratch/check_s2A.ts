import { db } from '../infrastructure/firebase/index'; async function run() { const doc = await db.collection('live_sessions').doc('s2A7CyRuWMKrJZFOJbvE').get(); if (doc.exists) { console.log('live_sessions doc exists. joinedParticipantsDetails:', doc.data().joinedParticipantsDetails); } else { console.log('No such doc in live_sessions'); } process.exit(0); } run().catch(e => { console.error(e); process.exit(1); });
