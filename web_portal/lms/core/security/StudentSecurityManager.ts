export class StudentSecurityManager {
  static init() {
    // Security features removed per user request
    // Screenshot and screen capture are fully allowed
  }

  static destroy() {
    // No-op: nothing to clean up
  }
}
