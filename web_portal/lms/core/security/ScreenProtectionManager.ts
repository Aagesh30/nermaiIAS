export class ScreenProtectionManager {
  static enable() {
    // Screenshot protection disabled per user request to allow captures
  }

  static disable() {
    // Screenshot protection disabled per user request to allow captures
  }
}
