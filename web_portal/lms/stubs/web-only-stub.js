// Web-only stub for native (Android/iOS) builds.
// Packages that depend on browser APIs (window, document, IndexedDB, etc.)
// are redirected here during native compilation to prevent Gradle build failures.
module.exports = {};
